# Rename to .env for local dev. Never commit your real .env
# Vite exposes only variables prefixed with VITE_. Treat them as PUBLIC.

# Example: public client-side keys (still consider them sensitive)
VITE_UNSPLASH_ACCESS_KEY=
VITE_PEXELS_API_KEY=

# Example: Sentry (public DSN)
VITE_SENTRY_DSN=

# Server/CI-only secrets (do NOT use on client; configure in CI/CD)
# VERCEL_TOKEN=
# TODOIST_CLIENT_SECRET=
# CALDAV_PASSWORD=
